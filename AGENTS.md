# HackathonGame 仓库指令

## 适用范围与事实来源

- 本文件适用于整个仓库；更深目录只有在确有模块专属规则时才添加嵌套 `AGENTS.md`。
- 本仓库中的每个任务都必须先使用 `.agents/skills/hackathongame-project/SKILL.md`，包括只读分析、文档或配置、工具接入、测试、审查以及代码或资源修改。如果 Skill 未被自动选择，直接按路径读取后再进行实质工作。使用该 Skill 不代表获得额外修改权限。
- 项目目标环境是 Godot 4.6、GDScript、GL Compatibility；主项目入口以 `project.godot` 为准。
- 本机当前使用 Godot 4.6.3；Godot 4.6.2 与 4.6.3 的补丁差异对本项目现有功能几乎没有实质影响，因此仓库契约只锁定 4.6 分支，不锁定补丁版本。
- 项目目标版本优先于任何通用或第三方 Skill 的版本假设。使用版本敏感 API、属性、命令行参数或导出设置前，按 Godot 4.6 文档和当前本机 4.6 运行时核验；不得因 Skill 示例面向更高版本而升级引擎、切换渲染器或改变脚本语言。
- `TECHNICAL_ARCHITECTURE_REPORT.md` 是唯一架构文档。涉及架构、场景生命周期、跨模块数据流、正式关卡或全局状态时，先完整读取该文件及直接相关代码。
- 架构契约、主线流程或已确认风险发生变化时，更新原架构报告；不要创建并行的架构总结文件。
- `README.md` 用于项目介绍，不能替代当前代码和架构报告作为实现依据。

## 修改授权与保护边界

- 分析、审核、诊断、Skill/MCP 配置和其他准备任务默认保持游戏内容只读；只有任务明确要求实现修改时才改代码或资源。
- `LevelModule/Formal/`、`PlayerModule/Formal/`、`EnemyModule/Formal/` 中的正式场景及其运行依赖属于受保护内容。基础设施配置、只读测试或 MCP 冒烟测试不得修改它们；正式场景修改必须由当前任务明确授权。
- MCP 首次接入只允许读取编辑器状态、场景树、属性、日志和截图。写入能力先在隔离测试场景验证，再按任务授权开放。
- 不要手工修改 `.godot/`、导入缓存、生成产物或 `.import` 文件。不要生成、复制或复用不明确的 Godot UID。
- `LevelModule/Backup/` 只作历史参考；不要新增正式运行依赖，也不要从中复用资源 UID。
- 保留用户已有改动，保持差异小而聚焦；不要顺手重排场景、批量格式化或改写无关文件。
- 安装第三方 Skill、插件或依赖前，核对来源、版本和包含的脚本。项目专用 MCP 优先配置在 `.codex/config.toml`，保持仅本机回环访问，不在仓库中保存密钥。

## MCP 协作与能力边界

- `.codex/config.toml` 是只对当前受信任仓库生效的项目级配置，应与 `addons/godot_ai/`、`project.godot` 和 `.gitignore` 一起纳入版本控制。不得为了本项目把 `godot-ai` 写入用户级 `~/.codex/config.toml`；详细协作说明见 `.codex/README.md`。
- Codex 的 `enabled_tools` 是客户端白名单，不是 Godot AI 服务端的全局权限。其他 Agent/MCP 客户端不会自动继承，必须分别配置等价白名单；不得因服务只监听回环地址就假定所有本地客户端权限相同。
- 项目级配置只限制 Codex 自动加载，不是运行中 MCP 服务的项目沙箱。其他本地客户端或 Godot 编辑器会话可能复用同一端口；任何写入前必须从编辑器状态和会话列表核对 `HackathonGame`、项目路径与当前场景，并显式激活正确会话。
- MCP 工具授权按工具而非目录生效，不能自动保护 `Formal/` 或阻止访问 `Backup/`。正式内容保护仍以本文件、Project Skill、当前任务授权和写入审批为准。
- Godot AI 3.1.5 的 `node_manage` 还包含分组添加/移除；没有明确需求时不得使用这些附带操作。`tileset_manage` 当前只读，不得声称能够通过 MCP 写入 TileSet。
- MCP 隔离写入测试完成后，除非明确批准为长期回归夹具，否则删除测试场景、截图、日志和其他临时文件；保留夹具时必须纳入 Git，并确认正式场景链与 Autoload 不依赖它。
- Godot AI 插件升级必须同步审查插件源码、服务器版本、客户端白名单和导出剥离行为，并作为一次项目提交交付；不要让个人的一键自更新造成团队版本漂移。
- 回环 MCP 不等于完全离线。Godot AI 会检查上游更新，遥测默认启用；涉及网络出站或隐私要求时先核对并按任务要求关闭，不能把“只监听 127.0.0.1”描述为“无任何外部网络访问”。
- 项目内 Skill 的可调用交叉引用只能指向当前仓库随附或当前环境可发现的 Skill；不具备可调用对象的领域边界必须写成普通说明。发现缺失路由时先修正文档或报告，不得自行安装额外 Skill，也不得声称调用了不存在的 Skill。

## 架构约束

- 跨模块通信优先经过 `EventBus` 和 `GameManager`。玩家或敌人不得反向依赖具体关卡脚本。
- 只有确需跨场景保留的状态才能进入 `GameManager`，并且必须有明确的初始化与转场清理路径。
- 敌人依赖 `EnemyBase._ready()` 自动注册；生成器不得在 `add_child()` 后重复手工注册。
- 资源路径必须使用 `res://` 并保持与磁盘文件名完全一致的大小写，以兼容 Windows、Web 和 Linux。
- 当前主线存在 `MainEntry` 托管与整树切换两种生命周期。没有明确架构决策时，不得擅自统一或扩散其中一种模式。
- 修改基类生命周期、死亡流程或转场清理时，同时检查 Boss 重写、HUD、输入锁、暂停、音频和跨关卡状态。
- 数值平衡优先进入 `DataConfig/` 的 Resource；不要继续把可配置数值散落到大型关卡脚本中。

## 工作方式

- 修改场景前先确认场景、附加脚本、外部 Resource、信号和 Autoload 依赖，不根据节点名猜测连接关系。
- 优先复用现有基类、Builder、配置 Resource 和事件协议；不要在关卡脚本中复制已有系统。
- 对大型关卡脚本采用局部、可验证的修改。未经明确要求，不进行顺带重构或主线转场迁移。
- 资产、对白和策划数值不完整时，报告缺口并安全降级；不要自行创作文案或虚构资产路径。
- 如果修改改变了公开契约、状态键、事件 payload、主线顺序或资源边界，同一任务中同步更新 `TECHNICAL_ARCHITECTURE_REPORT.md`。

## 验证要求

- 任何文件修改后至少执行 `git diff --check`，并检查 `git status --short` 与实际差异范围。
- GDScript、场景或 Resource 修改使用当前本机 Godot 4.6 可执行文件做 headless 主场景启动，并实例化受影响场景。
- 转场或全局状态修改还要验证正式主线中的相邻场景切换，以及输入、暂停、对话、音乐、玩家和敌人引用的清理。
- Shader、动画、音频和视觉布局修改必须追加真实图形环境或人工试听/画面检查；headless 结果不能替代视觉验证。
- 如果所需 Godot 可执行文件、资产或图形环境不可用，明确列出未完成的验证，不得声称已经通过。
- 最终交付说明修改文件、已执行检查、结果及剩余警告；不要把强制短时退出日志直接描述成正常游玩崩溃。
