# 2026-07-02

## 复战关卡 HUD 计时器
- 为 `LevelFuzhan01` / `LevelFuzhan02` 两个复战关卡添加了 HUD 计时器功能
- `UI/HUD.gd`: 新增计时器变量、右上角 UI 构建（980,20 位置，280x36 深色背景 + 金色文字）、`start_timer()`/`stop_timer()` 方法、`_format_time()` 精确到厘秒(MM:SS.cs)
- `Level_fuzhan_memory_base.gd`: `_load_hud()` 中存储 `_hud` 引用并调用 `start_timer()`；新增 `_stop_timer()` 辅助方法，在 `_complete_area()`、`_on_player_died()`、`_cleanup()` 三处停止计时器
- 计时逻辑：`_process(delta)` 中累积 delta，`get_tree().paused` 为真时跳过，保证与游戏暂停联动
