# 2026-06-20 工作日志

## Level_01 新增右侧边缘黄色闪烁（提示床的位置）
- 需求：手机交互完进入 GLITCH_TRANSIT 后，在屏幕右侧启动黄色闪烁提示床的位置，与左侧闪烁提示手机对称
- `Level_01_UIBuilder.gd`：新增 `_build_right_edge_flash()`，镜像左侧实现（PRESET_RIGHT_WIDE，offset_left=-8/-30），在 `build_all()` 中调用
- `Level_01.gd`：
  - 新增字段 `_right_edge_flash` / `_right_edge_glow` / `_right_edge_flash_active`
  - 新增 `_start_right_edge_flash()` / `_stop_right_edge_flash()`（镜像左侧）
  - `_check_flash_target_in_view()` 通用化：左侧激活检测手机入镜，右侧激活检测床入镜
  - `_trigger_climax_transition()`：进入 GLITCH_TRANSIT 解冻玩家后调用 `_start_right_edge_flash()`
  - `_on_final_bed_trigger()`：入口调用 `_stop_right_edge_flash()`
  - `_process` 检测条件改为 `_left_edge_flash_active or _right_edge_flash_active`

## Level_01 新增床交互空闲提示（电脑解锁前）
- 需求：电脑解锁前每次睡完床，若 2s 内未再找床互动，弹出叙事"没什么事做，还是继续睡觉吧"
- 新增字段 `_idle_bed_prompt_armed` + 常量 `IDLE_BED_PROMPT_TEXT` / `IDLE_BED_PROMPT_DELAY=2.0`
- `_maybe_arm_idle_bed_prompt()`：电脑未解锁时调用 `_arm_idle_bed_prompt()`
- `_arm_idle_bed_prompt()`：设 armed=true，await 2s 计时器，到期且状态合法（BEDROOM/电脑未解锁/未交互叙事睡眠）则 `_show_narrative` 弹提示
- `_disarm_idle_bed_prompt()`：armed=false
- 触发点：`_trigger_sleep_cycle` 两个结束分支（有/无 overlay）末尾调用 `_maybe_arm_idle_bed_prompt()`
- 取消点：`_trigger_sleep_cycle` 入口 + `_show_narrative` 入口均调用 `_disarm_idle_bed_prompt()`（覆盖再睡床、看 notice/thermos 等情况）
- 第4次睡眠后电脑 is_active=true，`_maybe_arm_idle_bed_prompt` 直接返回不再 arm

## 修复：动画冻结问题（PlayerBase 统一处理，已修复）
- 根源：旧 `_freeze_player` 调 `set_physics_process(false)` 连带禁用子类 `_on_physics_process` 中的 `_update_animation()`，导致 `_change_state(IDLE)` 后无人切动画，walk 持续循环
- 方案：PlayerBase 新增 `set_frozen(freeze: bool)` 统一方法
  - 冻结：velocity=0 + set_physics_process(false) + set_process_input(false) + _change_state(IDLE) + 显式 call("_update_animation") 刷新到 idle
  - 解冻：set_physics_process(true) + set_process_input(true)
  - 用 `has_method("_update_animation")` 守卫，兼容无动画的子类
- 7 个关卡 `_freeze_player` 改为 `player.set_frozen(freeze)`，旧代码注释保留（标注"保留以备回退"+回退说明）
- 关卡特定逻辑（交互物 monitoring 循环）保持不变
- 旧版本保留方式：PlayerBase.set_frozen 注释含完整旧实现 + 各关卡 _freeze_player 注释含旧代码块
- 8 文件 lints 均无错误

## 禁用关卡1技能图标"技"UI 显示（已修复）
- 需求：关卡1是叙事关卡，禁用右下角技能冷却图标显示
- HUD.gd 新增 `_skill_icon_suppressed` 标志位 + `suppress_skill_icon(bool)` 公开方法
- `_update_skill_cooldown()` 开头检查标志位，suppressed 时强制隐藏容器并 return（不被每帧 _process 覆盖）
- Level_01.gd `_load_hud()` 加载后调用 `hud.suppress_skill_icon(true)`（has_method 守卫）
- 不影响其他关卡（默认 false），不破坏 HUD 血条/暂停面板等事件驱动功能
- 2 文件 lints 均无错误

## 禁用 Level_02_03（现实房间/sub01）技能图标显示（已修复）
- 需求：现实房间是叙事段（已禁用 can_dash/can_skill），同样禁用技能UI
- Level_02_03.gd `_load_hud()` 加载后调用 `hud.suppress_skill_icon(true)`（has_method 守卫）
- 复用 HUD.gd 已有的 suppress_skill_icon 机制，无需改 HUD
- 1 文件 lints 无错误

## Level_01 黑屏淡入 + Level_05 遮罩优先级修复（已修复）
- 需求：lv1 首关加载加黑屏淡入；lv4/lv5 的转场黑屏没展示
- 根因：Level_05.tscn 的 CanvasLayer layer=100，与 MainEntry 转场遮罩 _transition_canvas layer=100 相同
  - layer 相同时后添加的渲染在上，Level_05 加载后其 CanvasLayer 盖住 MainEntry 遮罩
  - Level_04 的 CanvasLayerUI layer=2 不受影响，但为统一也受益于遮罩提升
- MainEntry.gd 两处改动：
  1. `_show_level_switch_overlay` 的 _transition_canvas.layer 从 100 → 1000（保证盖住所有关卡 CanvasLayer）
  2. `_load_formal_level` 首关加载前调用 `_show_level_switch_overlay(BLACK)`，加载后 `await _fade_out_level_switch_overlay()` 淡出（复用现成方法，3行）
- Level_04/05 的转场淡入现在能正常展示（遮罩 layer 1000 > 关卡 CanvasLayer layer 100）
- 确认：上一轮 layer 100→1000 修改已一次性覆盖 lv3/4/5 全部（lv3/4 CanvasLayerUI layer=2，lv5 layer=100，均 < 1000）
- lv3→lv4→lv5 切换均走 MainEntry._switch_to_level，复用同一 _show_level_switch_overlay(layer=1000)
- 1 文件 lints 无错误

## 深入排查 lv3/4/5 黑屏淡出不生效（第二轮修复）
- 用户反馈：layer 提升后淡出仍不生效
- 排查发现 3 个可能根因，一并修复：
  1. ColorRect 在 CanvasLayer 下 PRESET_FULL_RECT 可能首帧 size=0 → 显式设 size=视口大小 + position=ZERO
  2. create_tween() 绑定到 MainEntry 节点，若节点状态异常 tween 不执行 → 改用 get_tree().create_tween() 绑定到 SceneTree
  3. 加 print 调试日志（_fade_out 开始/完成时 alpha 值）便于用户定位
- 排查过程中验证：SleepOverlay 同样用 PRESET_FULL_RECT 且工作正常 → size 可能非根因，但显式设置无害
- 排查 _switching_level 卡死可能：_switch_to_level 第83行重置，正常流程不会卡
- 排查关卡 _full_cleanup：只清理关卡自身，不影响 MainEntry
- 用户决定搁置此问题，已清理调试 print，保留无害改进（layer 1000 / 显式 size / get_tree().create_tween）

## Level_03/04/05 入场黑屏淡入（关卡自身实现，已修复）
- 需求：lv3/4/5 入场加黑屏淡入，时长 1.5s，和方案A给Level_01加的一样方式
- 方案：不依赖 MainEntry 遮罩，各关卡 `_on_ready` 自行创建遮罩
- 每个关卡新增两个方法：
  - `_play_intro_fade_in()`：_on_ready 开头调用，创建 CanvasLayer(layer=2000) + ColorRect(满黑, size=视口, PRESET_FULL_RECT)
  - `_finish_intro_fade_in()`：_on_ready 末尾（set_process后）调用，get_tree().create_tween() 淡出 alpha 1→0 (1.5s TRANS_SINE)，完成后 queue_free 遮罩
- layer=2000 确保盖住所有关卡 CanvasLayer（lv5 最高 100）
- 插入点：Level_03 (118行/184行后)、Level_04 (144行前/188行后/203行前)、Level_05 (183行后/256行后/287行后)
- 3 文件 lints 均无错误

## 定位：Level_02 幽灵碰撞体 (2392,472) 根源（未修复）
- 症状：lv2 中 (2392,472) 附近有碰撞体，节点树已删但仍存在
- 根源：`Level_02_SceneBuilder.gd:33-36` `_build_dream_world()` 用 `_ensure_static_body` 动态创建 4 个 StreetBlocker
  - StreetBlocker01 (2468,472) size(264,16) 覆盖 x[2336,2600] y[464,480] → 命中 (2392,472)
  - StreetBlocker02 (2369,332) / 03 (2528,344) / 04 (2612,344)
- Level_02.tscn 的 CollisionBodies 下只有 MainAtticFloor/AtticLeftWall/AtticDoorWall/StreetGround，无 StreetBlocker
- `_ensure_static_body` 逻辑"不存在就创建"，故 tscn 删除无效，每次运行重建 → 幽灵碰撞体
- 待用户确认是否删除这 4 行

## 删除 Level_02 的 StreetBlocker + sub02_portal 入口（已修复）
- 删除 `Level_02_SceneBuilder.gd` 4 个 StreetBlocker（StreetBlocker01~04 动态碰撞体，StreetBlocker01 命中 2392,472）
- 删除 sub02_portal 传送入口（通往空场景 Level_02_sub02.tscn），涉及 7 处：
  - SceneBuilder: 创建 Sub02Portal 交互物的 5 行
  - Level_02.gd: 字段 `_sub02_portal_node`、常量 `SUB02_SCENE_PATH`、`_all_interactives` 引用、`_get_interactive_by_id` 分支、`_transition_to_sub02()` 方法
  - Level_02_FSM.gd: `"sub02_portal"` match 分支
- Level_02 主流程不受影响：玩家仍通过 LevelExitTrigger(5256,460) → Level_02_01
- 3 文件 lints 均无错误

## 安装 EdgeOne Makers Tools 技能集
- 来源：https://github.com/TencentEdgeOne/edgeone-makers-tools（因网络限制通过 zip 下载安装）
- 安装到 `~/.codebuddy/skills/`，共 8 个技能：makers-deploy / makers-agents / makers-edge-functions / makers-cloud-functions / makers-storage / makers-middleware / makers-cli / makers-recipes
